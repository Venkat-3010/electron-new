# Database Configuration
# Set DB_TYPE to 'sqlite' (default) or 'mssql'
DB_TYPE=sqlite

# MSSQL Configuration (only needed when DB_TYPE=mssql)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=electron_crud_db
DB_USER=your_username
DB_PASSWORD=your_password

# Environment
NODE_ENV=development
